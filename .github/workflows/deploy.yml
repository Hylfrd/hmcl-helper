name: Apostrophe's HMCL Helper deploy workflow

on:
  push:
    tags:
      - "*"
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy-and-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4
        with:
          ref: "master"

      - uses: actions/setup-node@v3
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm install

      - name: Build Site
        run: npm run docs:build

      - name: Deploy for Github 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: prod
          folder: docs/.vitepress/dist  # 确保路径正确
          single-commit: true
          clean: true
          token: ${{ secrets.GITHUB_TOKEN }}  # 确保使用正确的密钥
